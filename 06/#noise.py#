import numpy as np
import matplotlib.pyplot as plt
from scipy import optimize
from scipy import fft
import pandas as pd

d1=pd.read_csv('/home/gb980061/repo_metodi_computazionali/06/data_sample1.csv')
d2=pd.read_csv('/home/gb980061/repo_metodi_computazionali/06/data_sample2.csv')
d3=pd.read_csv('/home/gb980061/repo_metodi_computazionali/06/data_sample3.csv')
''' note
d1['time']=t
d2['meas']=y
modifica dy
'''
plt.plot(d1['time'],d1['meas'], color= 'red', label='d1')

plt.xscale('log')
plt.xlabel('X')
plt.ylabel('conteggi')
plt.grid()
plt.minorticks_on()
plt.legend(fontsize = 12, loc = "upper left", frameon=False)
plt.tight_layout()
plt.show()

'''
def f_fit(f,b):
    return f**-b

dy=np.ones(len(d1['time']))
par=1
par,parcov=scipy.optimize.curve_fit(f_fit,d1['time'],d1['meas'],p0=[par],sigma=y, absolute_sigma=True)
'''